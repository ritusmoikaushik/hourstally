# www and http are not handled here. Cloudflare Pages ignores a source with a
# host in it ("domain-level redirects: not supported"). They are zone settings:
# Always Use HTTPS, and a Redirect Rule from www to the root - both in the
# Cloudflare dashboard, both set by the founder. Verified 2026-09-21 that the
# three rules that used to sit here did nothing.

/index.html / 301
/time-to-decimal.html /time-to-decimal 301
/privacy.html /privacy 301
/about.html /about 301
/how-it-counts.html /how-it-counts 301
/with-lunch.html /with-lunch 301
